## python-binance==1.0.19  # comentado: no se usa en este despliegue / causaba fallos por proxy
## MetaTrader5==5.0.45
## NOTE: MetaTrader5 requiere instalación manual en algunos entornos Windows
## Si estás detrás de un proxy o pip no puede descargarlo, instala manualmente
## Ejemplo: descargar la rueda oficial o usar el instalador del proveedor, luego:
## python -m pip install path\to\MetaTrader5-5.0.45‑cp38‑cp38‑win_amd64.whl
pandas>=1.3.0
scipy==1.5.4        # Añadido: versión compatible con Python 3.8
pytz>=2021.3

## Requisitos principales para el bot (versión compatible con Python 3.8)
# NOTA: Si puedes usar Python >= 3.9/3.10, actualiza las versiones a las más recientes

## MetaTrader5==5.0.45  # comentado: instalar manualmente si necesitas trading en vivo
numpy>=1.21.0
pandas>=1.3.0
xgboost==1.5.2
# scipy==1.5.4  # comentado temporalmente: instalar manualmente (rueda/Conda) si necesitas funcionalidades numéricas
# scikit-learn==0.24.2  # comentado temporalmente: instalar con Conda o rueda compatible
xgboost==1.5.2
joblib>=1.0.0
requests>=2.25.1
python-dateutil>=2.8.1
pytz>=2021.3

# Herramientas de construcción / utilidades
pyinstaller>=5.1

# Si actualizas a Python >=3.9, puedes usar versiones más recientes de sklearn/scipy
# Ejemplo (opcional): scikit-learn>=1.0 scipy>=1.7

# Notas de instalación (Windows):
# - MetaTrader5: puede necesitar la rueda oficial o instalación desde el instalador del proveedor.
# - Si tienes problemas binarios con scipy/xgboost, usar Conda/Miniforge suele simplificar la instalación.